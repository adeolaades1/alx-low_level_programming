#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @one: first number input
 * @two: seconf number input
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
